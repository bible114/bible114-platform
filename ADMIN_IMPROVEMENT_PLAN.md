# ADMIN_IMPROVEMENT_PLAN.md — 관리자 화면 개선 설계서

> 작성: 2026-07 (계획 세션). 구현은 Sonnet 담당, 이 문서만 보고 구현 가능하도록 작성.
> **확정 범위**: 교회 관리자·플랫폼 관리자 **두 화면 모두** 개선. **시간축 추세/성장 차트는 제외**
> (일별 통계 스냅샷 인프라 불필요). "현재 현황 + 관심 필요 명단 + 목록 대개편 + UI 리프레시"에 집중.
> Firebase **compat** SDK 유지(`db.collection(...)`). 기존 대시보드 디자인 언어(indigo 그라데이션, rounded-3xl 카드) 계승.

---

## 재사용할 기존 자산 (새로 만들지 말 것)

- `src/utils/statsUtils.js` `calculateSubgroupStats` — 부서/소그룹별 통계 계산기(현재 관리자 화면에서 미사용). 대시보드에서 활용.
- `src/utils/exportUtils.js` `downloadCSV` — 현재 플랫폼 관리자만 사용. 교회 관리자에도 재사용.
- `users/{uid}/history/{id}` 서브컬렉션 — 읽기 기록(date, day, score). 교인 상세의 기록 표시에 사용.
- user 문서 필드: `currentDay, readCount, score, streak, lastReadDate, departmentId/Name, subgroupId/Name, isDeleted`.
- 진행도 계산 공식(기존): `총진행일 = (readCount-1)*365 + currentDay`.

---

## Phase A — 공통 관리자 UI 컴포넌트 (두 화면의 토대)

`src/components/admin/` 신설, 두 관리자 화면이 공유:

1. **`StatCard.jsx`** — 큰 숫자 + 라벨 + 보조수치 + 아이콘. indigo 그라데이션 강조 버전 지원.
2. **`AdminDataTable.jsx`** — 재사용 테이블: props로 `columns`, `rows`, 내장 검색창(텍스트), 정렬(헤더 클릭), 페이지네이션(기본 50/page), 다중선택(checkbox + 선택 액션바 slot), 행 클릭 콜백. 모바일에서 카드형으로 자동 전환(반응형).
3. **`SlideOverPanel.jsx`** — 우측 슬라이드 상세 패널(교인 상세용). 모바일에선 전체화면 시트.
4. **`ConfirmDialog.jsx`** — 파괴적 작업 확인(현재 `confirm()`/`alert()` 대체). 위험 작업은 빨강 강조.
5. **`Toast.jsx` / `useToast`** — 성공/에러 토스트. 현재 조용한 `console.error`를 화면 피드백으로.
6. **`ProgressBar.jsx` / `DonutStat.jsx`** — 읽기율 등 비율 시각화(외부 차트 라이브러리 없이 SVG/CSS).

원칙: 외부 차트 라이브러리 추가 금지(번들 이미 큼). 막대·도넛은 CSS/SVG로 자작.

---

## Phase B — 교회 관리자 화면 개편 (`ChurchAdminView.jsx`)

### B-1. 탭 구조 변경
`대시보드(신규·기본) / 교인 관리(개편) / 조직 / 공지 / 설정`

### B-2. 대시보드 탭 (신규) — 목양 한눈 보기
- **상단 StatCard 4개**: 전체 교인 / **오늘 진도(어제 이 시각 대비)** / 최근 7일 읽기율 / 평균 진행 DAY
  - ⭐ **핵심 지표 변경(사용자 피드백)**: "오늘 읽음 N명" 절대수는 조회 시점(낮/밤)에 따라 오해를 부름 →
    **"지금 시각 기준 오늘 N명 · 어제 이 시각 M명 (+/-차이)"** 로 표시해 진도 추세를 즉시 판단.
  - **선행 데이터 변경 필요(Phase 0)**: 현재 읽기 기록은 날짜만 저장(`history.date = toDateString()`, [useUserBibleActions.js:90](src/hooks/useUserBibleActions.js#L90)).
    시간대 비교를 위해 `handleRead` 트랜잭션의 historyItem에 `ts: serverTimestamp()` 한 필드 추가(하위호환, 기존 쿼리 영향 없음).
    firestore.rules의 history create는 필드 제약이 없어 별도 규칙 변경 불필요.
    **소급 불가**: 적용 다음 날부터 "어제 이 시각" 정상 작동. 그 전까지는 "오늘 누적 vs 어제 최종"으로 대체 표기.
  - 시간대 비교 계산: 오늘/어제 각 교회의 history를 `ts` 기준으로 "현재 시각(HH:MM) 이전" 카운트. 교회 규모가 크면
    비용 절감을 위해 집계 문서(`churches/{id}/stats/dailyReaders`에 시간 버킷) 도입을 검토하되, 1차는 직접 집계로 시작.
  - "최근 7일 읽기율" = 최근 7일 내 `lastReadDate` 보유 인원 비율(간단 버전). 정확한 주간 추세는 범위 밖(확정).
- **부서별 현황 카드 그리드**: `calculateSubgroupStats` 결과로 부서별 읽기율 막대 + 평균 진행 DAY + 인원. 소그룹 펼치기(accordion).
- **⭐ 관심 필요 명단** (목양 핵심): 3개 리스트, 각 행 클릭 → 교인 상세 패널
  - `7일 이상 미독`: `todayStr - lastReadDate >= 7일` (lastReadDate 없으면 미독으로)
  - `진행 하위 10%`: 총진행일 기준 하위 10% (최소 표시 조건은 구현 시 조정)
  - `최근 7일 신규 가입`: `createdAt` 최근 7일
  - 계산은 `statsUtils.js`에 `computeAtRisk(members, todayStr)` 헬퍼 추가해 재사용.
- **이번 주 스트릭 리더 Top 5**: `streak` 내림차순.

### B-3. 교인 관리 탭 개편 (`AdminDataTable` 적용)
- **툴바**: 이름 검색 / 부서 필터(드롭다운) / 읽기상태 필터(오늘읽음·미독·7일+미독) / 정렬 유지 / **CSV 내보내기**(`downloadCSV` 재사용)
- **페이지네이션** 50/page (수백 명 대비)
- **다중 선택 → 일괄 액션바**: 일괄 소그룹 배정 / 일괄 비밀번호 초기화(확인 다이얼로그)
- **행 클릭 → 교인 상세 SlideOver**:
  - 진행 요약(DAY, 독수, 점수, 스트릭, 마지막 읽은 날)
  - 읽기 기록(history 서브컬렉션 최근 N건)
  - 소그룹 변경 / 비밀번호 재설정 / 삭제(각각 ConfirmDialog)
- **보안**: 현재 편집행의 **비밀번호 평문 표시 제거**([ChurchAdminView.jsx](src/components/ChurchAdminView.jsx) 편집행) — "재설정"만 제공, 기존 값 노출 안 함.
- 삭제/복원은 유지하되 삭제 명단이 길어질 때 접기.

### B-4. 반응형 & 피드백
- 폰 레이아웃(목사님 폰 사용 잦음): 테이블→카드 전환, 하단 고정 액션.
- 모든 저장/삭제에 Toast, 파괴적 작업에 ConfirmDialog.

---

## Phase C — 플랫폼 관리자 화면 개편 (`PlatformAdminView.jsx`)

### C-1. 현황/교회 탭
- **교회 검색창** 추가(이름). 카드 그리드 유지하되 정렬(참여율/인원/이름).
- 각 교회 카드에 **관심 필요 인원 수** 뱃지 추가(computeAtRisk 재사용).
- 교회 드릴다운의 교인 테이블도 `AdminDataTable`로 교체(검색·필터·페이지네이션 공유).

### C-2. 회원(전체) 탭 — 성능 + 검색
- **검색**(이름/교회) + **필터**(교회 드롭다운, 읽기상태) + **페이지네이션**을 `AdminDataTable`로.
- **성능**: 현재 슈퍼관리자 로그인 시 `users`/`churches` 전체를 메모리에 적재([App.jsx](src/App.jsx) loadSuperAdminData). 이번 단계에서는
  최소한 **클라이언트 페이지네이션 + 검색**으로 렌더 부담부터 해소. (진짜 온디맨드 쿼리/서버 페이지네이션은 인덱스 추가가 필요한 확장 과제로 문서화만.)

### C-3. 감사 로그 (신규, 소규모)
- `adminActions` 컬렉션: `{actorUid, actorName, action, targetType, targetId, churchId, detail, at}`.
  - 파괴적/민감 작업에 기록: 교인 삭제/복원, 교회 삭제, 비번 재설정, 입장코드 변경.
- 동기화 탭 아래 **로그 뷰어**(최근 100건, 필터: 교회/작업유형).
- firestore.rules: `adminActions` create = churchAdmin·platformAdmin, read = platformAdmin(+ 본인 교회분은 churchAdmin). 필드 화이트리스트.

### C-4. 에러 표시 & 확인
- 조용한 `console.error`(동기화 실패, 디렉토리 재생성 실패 등)를 Toast/인라인 에러로 노출.
- 파괴적 작업(교회 삭제, seed 삭제)에 ConfirmDialog + 되돌리기 안내.

### C-5. (선택·후순위) 교회 가입 승인
- 100교회 근접 시 스팸 방지용. `churches.status: 'pending'|'active'`, 승인 대기 목록 + 승인 버튼.
- 이번 범위에선 **설계만**, 구현은 실제 필요 시점에.

---

## Phase D — 실제 운영 기능 (사용자 선택: 일괄 관리 · 가입/등록 · 공지·소통)

> 지금까지는 "보여주기(대시보드/목록)" 위주였음. 아래는 관리자가 실제로 **행동**하는 기능.

### D-1. 교인 일괄 관리 (Phase B-3의 일괄 액션 확장)
- 다중 선택 → **일괄 소그룹 배정 / 일괄 비밀번호 초기화 / 일괄 부서 이동 / 일괄 삭제·복원**.
- **CSV 가져오기**(신규): 이름·생년월일·부서로 교인 명단 업로드 → 미리보기 → 일괄 계정 생성.
  기존 개별 가입 흐름(`useAuth.buildNewMember`) 재사용, 중복(이름+생년월일) 검출.
- 모든 일괄 작업은 진행률·결과 요약(성공/실패 건수) + adminActions 기록.

### D-2. 가입/등록 관리
- **가입 대기·정리**: 부서 미배정(`departmentId` 없음) 교인을 한 곳에 모아 일괄 배정.
- **입장코드 관리 개선**: 코드 변경 이력, 마지막 변경일 표시, (선택) 코드 노출/숨김 토글.
- **잘못 가입 정리**: 중복 계정·오타 계정 병합/삭제 도우미.
- (교회 관리자 권한 밖인 승인 절차는 Phase C-5 참고 — 플랫폼 단.)

### D-3. 공지·소통 강화
- **대상 지정 공지**: 전체 대신 특정 부서/소그룹에만 공지 노출(`announcement.targets: [subgroupId]`).
- **공지 이력**: 지난 공지 목록·재사용, 게시 기간(시작·종료일) 설정.
- 카카오 링크 외에, 대시보드 관심 명단과 연동한 **원클릭 독려**(아래 E-1)로 연결.

---

## Phase E — 추천 기능 (내가 제안, 사용자 요청으로 추가)

> 효과·난이도를 표기. E-1/E-2/E-5를 우선 추천(효과 크고 난이도 낮음~중간), E-3은 인프라 커서 별도, E-4는 여유 시.

### E-1. ⭐ 미독 교인 원클릭 독려 (난이도 낮음)
- 대시보드 "관심 필요 명단"의 각 교인 옆에 **독려 버튼**: 카카오 채널/문자 링크 자동 생성(이름·미독일수 치환한 메시지 템플릿), 또는 (앱 알림 인프라 있으면) 인앱 독려.
- 개별 + 명단 전체 일괄 독려. adminActions 기록.
- 지금은 통계만 보고 끝나는데, 목양의 실제 액션을 화면에서 바로 하게 만드는 것이 핵심.

### E-2. ⭐ 소그룹 리더 권한 위임 (난이도 중간)
- 목사 혼자 수백 명 관리 불가 → **부서장/구역장에게 "자기 소그룹만" 보는 제한 관리 권한**.
- 데이터: `users.role`에 `groupLeader` 추가 + `leaderScope: {departmentId, subgroupId}`.
- firestore.rules: groupLeader는 같은 교회·같은 소그룹 교인만 read/제한 update(비번 재설정·독려 O, 삭제·코드변경 X).
- 대형교회·100교회 확장의 핵심(목양 분산). 로그인/권한 체계와 연결되므로 설계 신중히.

### E-3. 읽기 리마인더 푸시 (난이도 높음 — 별도 단계)
- PWA 푸시로 개인 설정 시간에 "오늘 말씀 읽으셨어요?" 알림 → 미독률 근본 개선.
- 인프라: FCM(웹푸시) 토큰 수집 + 서비스워커 푸시 수신 + 스케줄 발송(Cloud Functions/cron). 규모 있는 작업.
- 이번 범위에선 **설계·타당성만**, 구현은 별도 승인 후.

### E-4. 완독·연속 축하 자동화 (난이도 낮음~중간)
- 1독 완료·30일 연속 등 달성 시 자동 축하 카드/뱃지(+ 관리자 대시보드에 "이번 주 축하할 교인" 노출).
- 기존 achievements 데이터 활용. 동기부여 + 목사 수작업 감소.

### E-5. 주간 목양 리포트 (난이도 낮음 — 대시보드 계산 재사용)
- 매주 부서별 현황·관심 명단·완독자를 **한 장 리포트**로 정리(앱 내 보기 + 인쇄/이미지 저장, 선택적으로 카톡 공유).
- 심방·주보 준비에 바로 사용. computeAtRisk/calculateSubgroupStats 재사용.

---

## 범위 밖(명시적 제외)
- 시간축 추세/성장/DAU 차트 (일별 스냅샷 크론 인프라 필요 — 사용자 결정으로 제외).
- 교인 대상 앱 내 메시징(현재 카카오 외부 링크 유지).
- 서버 사이드 페이지네이션(대규모 온디맨드 쿼리) — 확장 과제로만 기록.

---

## 구현 순서 제안
0. **Phase 0** 데이터 변경: history에 `ts: serverTimestamp()` 추가(B-2 시간대 비교의 선행조건, 하루 먼저 배포할수록 좋음)
1. **Phase A** 공통 컴포넌트 (이후 전부 의존)
2. **Phase B** 교회 관리자 대시보드 + 목록 개편 (사용 빈도 최고 → 체감 효과 큼)
3. **Phase D** 운영 기능 (일괄 관리·가입/등록·공지) + **E-1 원클릭 독려**, **E-5 주간 리포트** (대시보드와 자연스럽게 연결, 난이도 낮음)
4. **Phase C** 플랫폼 관리자 (검색·드릴다운·감사 로그)
5. **E-2 권한 위임** (로그인/권한 체계 영향 → 신중히), 이후 여유 시 **E-4 축하 자동화**
6. **E-3 리마인더 푸시** — 인프라 별도 승인 후

각 Phase마다 `npm run build` 통과 + 커밋. 신규 규칙(adminActions, groupLeader)은 배포 전 기존 흐름 회귀 확인.

## 검수 기준(핵심)
- [ ] 교회 관리자 진입 시 대시보드에 오늘 읽기율·부서별 현황·관심 필요 명단이 뜬다
- [ ] 교인 목록에서 이름 검색/부서·상태 필터/페이지네이션/CSV 내보내기가 동작한다
- [ ] 교인 클릭 시 상세 패널에 읽기 기록·진행·스트릭이 보이고 비번 평문은 어디에도 안 뜬다
- [ ] 다중 선택 후 일괄 소그룹 배정/비번 초기화가 동작한다
- [ ] 플랫폼 관리자에서 교회/회원 검색이 되고 수백~수천 행에서 렌더가 버벅이지 않는다
- [ ] 삭제/비번변경 등이 adminActions에 기록되고 로그 뷰어에서 보인다
- [ ] 모든 파괴적 작업에 확인창, 실패 시 화면 에러 표시
